@model IEnumerable<PracticeADO_crud.Models.Employee>

@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Show All Employee Record </h1>
    
</div>

<div class="row">
   <table class="table table-hover table-condensed">
       <thead>
           <tr>
               <th>ID</th>
               <th>Name</th>
               <th>City</th>
               <th>Department</th>
               <th>Pin-Code</th>
               <th>Actions</th>
           </tr>
       </thead>
       <tbody>
           @*@foreach (System.Data.DataRow  item in ViewBag.emp.Rows)
        {
        <tr>
            <td>@item["emp_id"]</td>
            <td>@item["emp_name"]</td>
            <td>@item["emp_department"]</td>
            <td>@item["emp_city"]</td>
            <td>@item["emp_pincode"]</td>
        </tr>
        }*@

           @foreach (var item in Model)
           {
               <tr>
                   <td>@item.emp_id</td>
                   <td>@item.emp_name</td>
                   <td>@item.emp_department</td>
                   <td>@item.emp_city</td>
                   <td>@item.emp_pincode</td>
                   <td>
                       <a href="@Url.Action("EditEmployee","Home",new {id=item.emp_id })">Edit</a>
                       |<a href="/Home/Delete?id=@item.emp_id">Delete</a>
                   </td>
               </tr>
           }
       </tbody>
   </table>
</div>